import java.util.*;     

public class App {
    public static void main(String[] args) {

        // input
        Scanner sc = new Scanner(System.in); // System.out for output, System.in for input
        // not sc capturing the the input
        // String input = sc.next(); // next just can read single word
        
        //String inp2 = sc.nextLine();    // it will also count spaces
        //System.err.println(inp2);
        
        int num = sc.nextInt();    // to read no.
        System.err.println(num);
        
        /* OTHERS ARE
         * nextByte
         * nextFloat
         * nextDouble
         * nextBoolean
         * nextShort
         * nextLong
         */
    }
}
